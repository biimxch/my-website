export interface MetaItem {
  label: string;
  value: string;
}

export interface SubProject {
  id: number;
  title: string;
  image: string;
  concept: string;
  front: string;
  back: string;
}